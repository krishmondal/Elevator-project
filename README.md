# Elevator-project
This project is a simple simulation of an elevator system implemented in Java. It demonstrates the use of Object-Oriented Programming (OOP) principles and the Java Collection Framework. The program simulates an elevator in a building that processes requests to move to different floors.
